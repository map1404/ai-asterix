services:
  - type: web
    name: voice-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web_agent.py
    plan: free
    autoDeploy: true
    healthCheckPath: /healthz
